import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    popupInstallDesktop: {
        display: 'none',
        position: 'absolute',
        top: '4px',
    },
}));

export default useStyles;
