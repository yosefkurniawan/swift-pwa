import Core from '@plugin_optionitem/GroupedProduct/core';
import View from '@plugin_optionitem/GroupedProduct/view';

const GroupedProductOptionItem = (props) => (
    <Core
        {...props}
        View={View}
    />
);

export default GroupedProductOptionItem;
