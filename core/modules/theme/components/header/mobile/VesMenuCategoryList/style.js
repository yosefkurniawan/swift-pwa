import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    zeroLevel: {
        paddingLeft: '1rem',
    },
    firstLevel: {
        paddingLeft: '2rem',
    },
    secondLevel: {
        paddingLeft: '3rem',
    },
}));

export default useStyles;
