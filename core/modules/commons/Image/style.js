import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    image: {
        width: '100%',
        height: '100%',
    },
    hideImage: {
        display: 'none',
    },
}));

export default useStyles;
